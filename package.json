{
  "name": "h3-securemail-gmail",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "deps": "npm install",
    "stage": "mkdir dist || echo Dist already exists.",
    "bundle": "browserify -o dist/extension.js src/extension.js",
    "build": "npm run stage && npm run bundle"
  },
  "author": "H3 Digital",
  "license": "ISC",
  "dependencies": {
    "gmail-js": "^0.6.8",
    "jquery": "^1.11.0"
  },
  "devDependencies": {
    "browserify": "^13.3.0"
  }
}
